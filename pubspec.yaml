name: cixian
description: "词冼 - 背单词软件"
publish_to: 'none'
version: 0.10.0+17

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  http: ^1.2.2
  package_info_plus: ^8.1.0
  url_launcher: ^6.3.1
  flutter_tts: ^4.0.2
  dio: ^5.7.0
  path_provider: ^2.1.5
  open_filex: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/wordbooks/